# Submission endpoint (if using a custom webhook instead of Netlify Forms)
FORM_WEBHOOK_URL=your_webhook_url_here

# Netlify site ID (optional, for CLI deploys)
NETLIFY_SITE_ID=your_netlify_site_id_here
